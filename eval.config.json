{
  "runner": {
    "command": "openclaw",
    "mode": "dev",
    "comment": "Set mode to 'dev' for --dev flag, 'prod' for production. Change command to 'coco' if using coco CLI."
  },
  "dataset_dir": "eval/eval_dataset",
  "results_dir": "eval/llm_invoke_results",
  "openclaw_dev_dir": "~/.openclaw-dev",
  "openclaw_prod_dir": "~/.openclaw"
}